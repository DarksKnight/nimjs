import { Control } from "./Control"

export abstract class WindowBase {
    initWindow(): void;
    findControl(name: string): Control | null;
}

export class Window extends WindowBase {
    create(layout: string, name: string, style: number): void;

    showWindow(): void;

    centerWindow(): void;
}